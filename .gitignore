# Database backups: real data snapshots, not source - keep them out of version control.
supabase/backups/

# Local Claude Code tooling config - not part of the deployed site.
.claude/settings.local.json
.claude_scratch_bak/

# Windows folder metadata.
desktop.ini

